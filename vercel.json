{
  "version": 2,
  "buildCommand": "cd gdg_hack_report && npm install && npm run build",
  "outputDirectory": "gdg_hack_report/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
